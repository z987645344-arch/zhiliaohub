# 知了hub 改动记录
> 每轮完成改动后在此追加记录（新条目追加在**最前**，本文件为新在前的倒序）。
> 纯文档/流程整理的三段式补丁存档同样需要记录，不得省略。
> **最后追加：2026-08-18**

## 2026-08-18 手册新增 1.2.3「改动超出指令范围时怎么办」

- **把一次真实往返固化成三档口径**：① **不改就交付不了**——可以改，但必须同一轮显式报告改了什么、为什么、怎么回退；② **放宽任何安全相关规则**——即使已实测证明防护未削弱，**也必须显式请求确认，不得默认通过**，因为实测决定改动是否正确、不决定执行者是否有权替指挥师拍板；③ **扩大授权范围、动未授权的仓库或文件**——停下，不要改。
- **另立一条关于确认送达的规定**：指挥师的确认必须出现在**发给执行者的指令文本**里才算数。只存在于指挥师与用户对话中的「我已确认 / 已采纳」，执行者这边等同于没收到，应继续按未确认处理并显式追问，不因怕显得啰嗦而放弃。本节来源正是这样一次往返：执行者把 `!.env.example` 放宽为 `!.env*.example`（不改则新建的模板文件会被 `.gitignore` 静默排除、提交时无声消失），连续三轮记录「此事仍悬着」，而确认从未进入转发文本——**三轮追问事后证明每一轮都对**。
- **它自己就是自己的第一个适用场景**：这条口径此前只写在仓库外的指挥师手册里，而它约束的是执行者行为，与 `v2.1` 越权同一个根因——规则存在，但不在必须照做的那个角色读得到的地方。经核对指挥师手册确认：该条一度被写在那边正文，由执行者指出后才移入两份仓库手册，那边现在只留指向本节的索引、不再复制正文。
- **验证**：diff 只含新增该节 15 行（49–63，位于 1.2.2 之后、1.4 之前）与头部「最后更新」补列一项；剔除这两处后与上一次提交 `d9a4b5e` **逐行字节完全一致**。`1.2.3` 全文只命中自身标题一处，无编号冲突、无悬空交叉引用；双空行、分隔线前后空行、标题层级三项结构检查均通过；第十章与 8.1 未重复规定同一主题。**跨项目逐字节相同已在本侧证实**：该节正文（去标题与首尾空行）md5 前 16 位为 `13e1673bc7643e1d`，与另一项目实测值一致；另一侧由指挥师核对。
- 本轮按 8.1 不打标签，为普通工作条目、不含版本号。未改其他章节、未重排编号、未连接服务器、未碰本项目之外的仓库。

## 2026-08-18 补本机 Compose 验证能力：nginx / TLS / 路由不再到服务器才第一次运行

- **补的是一个有因果证据的盲区**：此前本机只能 `npm start`（Node 直接监听 3001），nginx 配置、TLS、路由、CSP 头与 Compose 编排**全都要到服务器才第一次真实运行**——此前两个证书问题（挂载方式写错、`standalone` 占不到 80 端口）本机跑一遍就能暴露。本轮交付 `.env.local.example`、`deploy/generate-local-tls.sh`（自签证书，SAN 覆盖 `localhost` 与 `lab.localhost`）与 `admin-server/deploy/README.md` 新增的「本机 Compose 验证」一节。
- **设计决定：同一份 `docker-compose.yml`、同一份 `deploy/nginx.conf`，只换 `.env`。** 核实 compose 已把全部可变项外置成 15 个环境变量，唯一写死的是容器内部地址与挂载目标（本就不该参数化），因此**不需要** override 文件。刻意不建 `docker-compose.local.yml`：那会制造第二个真相源，本机验的就不再是生产要跑的东西，这件事的意义直接归零。
- **本机实测通过的（全部真跑，不是审阅）**：`docker compose config --quiet` 退出码 0；镜像构建成功、两容器 healthy；`nginx -t` 通过；**容器内渲染后的配置确有 22 条 `set_real_ip_from`**（1.6 第 2 条要求的那道检查）。HTTP 8080 → 301；HTTPS 8443 的 `/`、`/index.html`、`/tools.html`、`/health`、`/admin/login` 全 200，`/health` 返回 `deployment: production`，不存在路径 404。`lab.localhost` 那条 server_name 独立生效：根目录 403（空 lab-storage 的正确行为）、`/health` 404 证明**它不反代到 Node**、CSP/CORP/Referrer-Policy/nosniff 四个头齐备、**0 个 Set-Cookie**；主站不带小作坊的 CSP。主站会话 Cookie 为 `HttpOnly; Secure; SameSite=Strict`——**这条同时证明 nginx 转发的 `X-Forwarded-Proto=https` 被后台采信了**，否则 Secure Cookie 在后台眼中的「HTTP 连接」上根本不会下发。
- **本机验不到的（已写成文档里最重要的一节，防止将来拿本机结果当线上证据）**：Cloudflare 在前的一切（22 条 CF 网段本机一条都不会命中、`CF-Connecting-IP` 还原、WAF、DDoS 吸收）、真实来源 IP 与限流分桶、真实 DNS 与证书信任链（本机自签）、生产 `.env` 真实值及其副作用、服务器 Compose **5.4.0** 与本机 **5.3.1** 的版本差、以及真实数据与负载。另记录一个本机专属现象：80→443 跳转的 `Location` 不带端口（nginx 用 `$host`），本机落到没在监听的 443——**这是端口映射的产物，不是配置缺陷，不要为迁就本机去改 nginx**。
- **过程中发现并修复一处会让本轮交付静默落空的问题**：`.env.local.example` 命中上一轮新加的 `.env.*` 规则**会被忽略**，提交时静默排除、文件永远进不了仓库。已把 `!.env.example` 放宽为 `!.env*.example`，并按 7.7 用落盘探针复验：`.env`、`.env.local`、`.env.bak-1`、`.env.production`、`admin-server/.env` 仍**全部被挡**，只有 `*.example` 模板放行；`runtime/` 整个目录与 `.env.local` 也确认被忽略，`git add -An` 证明只有 4 个预期文件会进暂存区。
- **凭据与清理**：本机 `admin-server/.env` 用的是机器上原有的一次性开发值，**未从服务器复制任何真实值，也未写进任何入库文件**；文档只写生成方法、不写结果。验证完已 `docker compose down`，无残留容器与网络，`runtime/` 与 `.env.local` 均不入库。本轮**未改** `docker-compose.yml`、`deploy/nginx.conf` 与任何应用代码，未连接云服务器，未碰 `zhiliaohub_app`。按 8.1 不打标签，本条为普通工作条目、不含版本号。

## 2026-08-18 手册新增 7.7「判断文件是否被 Git 忽略」

- **把上一轮踩到的工具陷阱写成规则**：判断文件会不会被 `.gitignore` 挡住，不能靠读 `.gitignore` 推断，也不能想当然用 `git check-ignore` 的退出码。三个坑：① 加 `-v` 后只要有任何规则命中就返回 **0**——**包括 `!` 开头的否定规则**，于是一个「本就不该被忽略」的文件同样返回 0，看起来像被忽略了；② 判断要用 `-q`，**0 = 被忽略、1 = 未被忽略**；③ **已跟踪文件默认被跳过、永远返回 1**，要测必须加 `--no-index`。拿不准时以真实文件为准：建一个未跟踪探针文件，看 `git status --ignored` 把它归进 `!!` 还是 `??`。
- **三条断言本轮全部实测证实，不是照抄文字**：① `-v` 对否定规则确实返回 0，不带 `-v` 才返回 1；② `-q` 对 `probe/.env` 返 0、对 `probe/.env.example` 返 1；③ 在一次性临时仓库里让同一个文件先未跟踪、后被跟踪，退出码由 0 变 1，加 `--no-index` 又变回 0——决定性地证实「已跟踪被跳过」这条。探针文件与临时仓库均已删除，两个真实仓库的索引未被触碰。
- **该节的来源是两个项目的执行者在同一轮各自独立踩到第 1、3 条**，且都没采信退出码、改用落盘探针实测证伪；正文两边逐字相同。它同时纠正了上一轮指令里「`.env.example` 应当退出码 1」的说法——该预期在本仓库碰巧成立，但成立的原因是那两个文件**已被跟踪**（第 3 条），不是否定规则生效；对未跟踪的同名文件，带 `-v` 时返回的是 0。
- 同时更新头部「最后更新」一行。手册 389 → 402 行；除 7.7 整节 13 行与头部一行外，全文其余部分与上一次提交 `9e97d26` **逐行字节完全一致**，已用剔除新增行后整文件比对核实；IPv4 与域名/主机名形串全文 0 命中，7 条分隔线前后空行齐备。本轮不打标签，按 8.1 写普通工作条目、不含版本号。

## 2026-08-18 两个仓库补 `.gitignore` 的 `.env` 衍生文件规则

- **实质变化是从「只挡两个确切位置」变成「挡任意层级」**：原有的 `/.env` 与 `/admin-server/.env` 都是**路径锚定**规则，只覆盖仓库根与 `admin-server/` 两处的 `.env` 本体——在别处（例如 `admin-server/config/`）新建 `.env`，或建任何 `.env.bak-*`、`.env.local`，都挡不住。现替换为不锚定的 `.env`、`.env.*`、`!.env.example` 三行；`/admin-server/.env` 因已被不带斜杠的 `.env` 覆盖而成为冗余，一并删除。
- **起因是一次真实暴露**：2026-08-16 在服务器改配置时，于仓库目录内建了 4 个含真实密钥的 `.env` 备份，而它们不被任何规则忽略，一次 `git add .` 就可能推进公开仓库。那 4 个文件已移到仓库外，但根因正是本条规则的覆盖面。
- **新发现：`zhiliaohub_app` 此前完全没有任何 env 规则**，连 `.env` 本体都不被忽略。原待办记载以为两个仓库都已覆盖，实测才发现不是。该仓库同轮补上同样三行，其 CHANGELOG 另有对应条目。
- **判断忽略规则一律以 `git check-ignore` 实测为准，不靠读 `.gitignore` 推断。** 改后逐条实测：两个仓库的 `.env`、`.env.bak-1`、`.env.local` 以及嵌套路径 `admin-server/config/.env`、`app/.env` 均被忽略；`admin-server/.env` 在删除那条冗余规则后**仍然被忽略**，未失效。
- **`.env.example` 的反例改用真实文件验证，没有只看退出码**：`git check-ignore -v` 在匹配到否定规则时同样返回 0，退出码会误导（不带 `-v` 才返回 1）。改为新建未跟踪探针文件、看 `git status` 如何归类，确认两个仓库的 `.env.example` 都仍会进入未跟踪列表、将来能被追踪；已跟踪的 `.env.example` 与 `admin-server/.env.example` 状态未变，探针文件已清理。本轮不打标签，按 8.1 写普通工作条目、不含版本号。

## 2026-08-18 手册补「地址与主机名的公开边界」，并把打标签从逐轮改为攒够再打

- **新增 1.8「地址与主机名的公开边界」**：本仓库是公开仓库，主站域名、小作坊子域名与知天的客户端主机名本就是公开入口，藏没有意义；**真正必须留在仓库外的是源站真实 IP**。站点由 Cloudflare 代理，但源站可被直连，IP 一旦公开，任何人都能绕过 CF 的 WAF 与 DDoS 吸收直接打源站——接 CF 之前 IP 泄露只是暴露服务器，接了之后它还额外作废一整层防护。知天的企业管理后台主机名同样禁止出现在本仓库任何文件中，需要指代时写「知天企业管理后台主机名」。
- **该节按本仓库自身情况另写，不是照抄知天**：此前这条边界只写在知天手册，本仓库只有散落在 5.6 等处的「不写真实凭据/IP」。**两个项目要保护的东西不同**——知天护的是一个刻意不公开的管理后台主机名，知了hub 护的是源站 IP——所以规则形态不同，共享的只有「规则不能只落单边」这个原则。
- **8.1 下新增「什么时候才打标签」：打标不再逐轮进行。** 提交与推送是安全网、每轮照常；标签是「值得回到这里」的锚点，攒够了再打。真实行为变化且准备部署、一个工作主题收尾、服务器部署前才打标；纯文档或流程整理累积若干轮后合并打一个三段式补丁号。不打标的轮次写**普通工作条目**（日期 + 概括，不写版本号、不暗示已存档），打标那一轮写**存档条目**并说明该标签覆盖了此前哪几条工作条目。
- **该约定由用户 2026-08-18 提出**，起因是标签列表被纯文档补丁淹没。核实 Git 标签后确认：2026-08-15 至 08-18 四天内，两个项目共产生 **9 个**三段式补丁标签（知了hub 4 个：`v2.0.1`、`v2.0.2`、`v2.1.1`、`v2.1.2`；知天 5 个：`v3.4.1`～`v3.4.5`），**没有一个标记出真正不同的运行状态**；而标签一经推送不删不重打，噪声会永久留在列表里。第八章本来就区分「存档条目 / 普通工作条目」，只是此前每轮都被当成存档条目处理——**规则在手册里，执行时没照做**。
- **本轮不打标签**，故本条为普通工作条目、不含版本号。手册现 388 行；除三处授权改动（1.8 整节 18 行、8.1 新增小节 16 行、头部「最后更新」一行）外，全文其余部分与上一次提交 `e3a2aac` **逐行字节完全一致**，已用剔除新增行后整文件比对的方式核实。

## 2026-08-17 工作手册瘦身：移出指挥师侧内容，只留执行者真正照做的规则

- **起因**：手册约一半篇幅是写给指挥师看的（职责、执行者选型、指令格式、工作流程），执行 agent 永远不会照做，却每轮开工都要通读。用户裁定按读者拆分——仓库内只留执行者规则，指挥师侧内容完整移到仓库外的指挥师手册，**没有任何内容被丢弃**。文件标题相应由「指挥师工作手册」改为「执行 agent 工作手册」。
- **删除与移动**：删掉第一章的指挥师职责 6 条与 1.1 核心分工原则（其中约束执行者的 5 条改写为新的开头原则保留下来）、1.2「执行 agent 选型与调度」、1.3「为什么仍保留决策/执行边界」的论证，以及第二章（新指挥师接手顺序）、第三章（标准工作流程）、第四章（指令格式与 4.1–4.4 范例）；1.2.1 由 7 条服务器操作红线缩写为「不归你」与「仍归你的受版本控制部署文件」两段。原 4.5「GitHub存档指令的标准骨架」的 6 条正文**一字不改**升为末尾的第十章「存档操作规范」，引导句改写为「默认由你完成（依据见 1.2.2）」，1.2.2 内两处指向 4.5 的引用同步改为指向第十章。
- **补回一条被误删的规则，并补上此前从未有过的一条**：新增 1.5「文档权威性原则」，正文取自原第二章末尾段一字不改——「文档是导航，不是比代码和运行结果更高优先级的事实源……先停止引用过期结论，列出差异」；它随第二章一起被删，而知天手册有等价规则且明确保留，两边一有一无——**这正是 1.2.2 引用块写的「规则不对称等于给违反留了缝」，也是当日越权事故的成因**。另新增 1.7「开工前的 Git 检查」：对本轮涉及的每个仓库分别 `git status` 与 `git log origin/<分支>..<分支>`，确认没有未推送的本地提交——本仓库此前**从来没有**这条强制检查，跨手册排查后由指挥师直接写入两个项目，规范正文经 SHA-256 比对确认两边逐字节一致。同时给第一章原则补第 7 条「你的自检不构成验收」。
- **新增 1.6「本机运行 compose 的约束」，来自知天执行者的跨项目上报，两个项目同轮同步补入、措辞逐字对齐**：原 1.2.1 的 7 条服务器红线里有 2 条与「在哪台机器上跑」无关、只与「跑 compose」这个动作有关，而执行者改部署文件时正会在本机跑。① 禁止输出完整 `docker compose config`（含真实 `env_file` 会展开全部密钥），语法验证只用 `--quiet`，**也不能用 `--format json` 再管道过滤**——密钥已进了进程输出，过滤只是没显示出来；② `docker compose up -d` 不重建已有标签镜像，也不因挂载内容变化而重建容器，必须显式 `build`、必要时 `--force-recreate`，「容器 healthy」不能单独作为版本正确的证据。第 ② 条的依据是本项目 v2.0 部署实测：`nginx -t` 通过、5 个入口全 200、容器 healthy，但渲染出的配置里 `set_real_ip_from` 一条都没有。
- **只搬不改，并已逐行验证**：移出的内容没有任何一条规则的含义被修改，新增的只有上面所列的 1.5、1.6、1.7 与第 7 条原则。1.2.2（除上述两处引用）、1.4、第五～九章全部 251 行、第十章正文 6 条与 HEAD 逐行字节比对**均 0 差异**——保留内容是从原文件切片拼装的，不是重新誊写；1.5 正文也与原第二章末尾段逐字节相同。**章节编号保留历史值、不重排**（一 → 五 → 六 …），因为 1.2.2、7.4 等编号被手册内部、本文件和仓库外文档交叉引用，重排会让这些引用全部作废；1.5–1.7 均为新号，未复用任何旧号。行数由 540 降到 354。仅协作文档结构调整，无代码、接口、数据结构或部署行为变化，按 8.1 节第 2 条走三段式补丁号 **`v2.1.2`**——`v2.1.1` 已在本日稍早用于「补 1.2.2 存档职责划分」并推送到远程，既有标签不删除重打，故第三段顺延递增。

## 2026-08-17 补「存档职责划分」到工作手册：起因是指挥师本人越权执行了 v2.1 存档

- **这条不是常规文档完善，是补一次真实违规暴露出的缝。** `v2.1` 的提交、推送与打标签由**指挥师本人**执行，而用户此前已明确指示交给执行 agent。用户随后说的「一起存档吧」回答的是**批次问题**（提交/推送/打标签三次授权是否合并），不是重新指定执行者，被指挥师误读为授权自己动手。
- **根因有两层，都不是记性问题。** 其一，`docs/claude_skill.md` 4.5 节**本来就写着**「Git 提交、推送和标签同样属于严格执行任务，通常优先交给作为执行者的 Claude Code」——指挥师本轮全程依据该手册办事（读其 1.2.1 与 7.4 来写规格），却没有在存档前回看这一条。其二，2026-08-16 确立的「存档职责划分」**只写进了 `zhitian/docs/claude_skill.md`，没有同步到本仓库**；本轮开工前实测确认本仓库该关键词命中 0 处、知天 1 处，规则不对称本身就是给违反留的缝。
- **改动只有一个文件**：`docs/claude_skill.md` 新增 `### 1.2.2 存档职责划分：现场记录与推送归执行 agent`，位置在第一章内、紧接 1.2.1 之后。放第一章而不是第四章，是因为它属于**职责边界**；4.5 节讲的是「存档指令怎么写」，两者不重复也不互相替代。同时把该文档头部的「最后更新」由 2026-08-15 改为 2026-08-17，避免文件自称的更新日期早于其内容。
- **新增内容的三方分工**：执行 agent 写 CHANGELOG 中「改了什么 + 实测证据」的部分（它在现场，指挥师只读 diff）；指挥师审阅并补充只存在于对话中的跨项目上下文、判断版本号、维护 `claude_memory.md`；**推送执行默认归执行 agent**，与 4.5 节一致。
- **针对这次误读专门写死了一条**：用户可以逐次改派推送执行者，但**必须是明确指定执行者**；用户对**批次或时机**的回答不构成改派。同时保留两条不可放宽的约束——不得把存档整体收归指挥师（执行 agent 的现场记录是一道独立检查，2026-08-16 当轮它就纠正了指挥师规格中的四处错误），以及提交、推送、打标签仍是三次独立授权。
- **确立一条跨项目规则**：凡是需要两个项目共同遵守的协作规则，必须同时写进两份 `claude_skill.md`，只写一份不算已确立。该结论以引用块附在 1.2.2 节末尾，与失误实例写在一起，避免日后只看到规则、看不到它为什么存在。
- **`v2.1` 不回滚。** 其内容经完整验收、部署已验证生效；为流程错误改写已推送的提交与标签，代价大于收益，也违反项目既定原则「既有标签一经推送不删除重打」。
- **同一存档还含指挥师维护的 `docs/claude_memory.md` 更新**（按 1.2.2 的分工，该文件归指挥师）：把 `v2.1` 已于 2026-08-17 部署上线并用日志实证的事实校准进来；记录部署时踩到的坑——`docker compose up -d` **不会重建 nginx 容器**，绑定挂载的模板改了源文件也不触发重建，当时 `nginx -t` 通过、入口全部 200、容器 healthy，但容器内渲染结果里 `set_real_ip_from` 是 0 条，必须 `--force-recreate` 并核对容器内渲染；重排因删除已完成项而断号的待办清单。并**特意保留**了 `v2.0` 存档后整整三天未被部署、直到随 `v2.1` 才生效的历史——那是「存档完成 ≠ 线上生效」最直接的一次实例。
- 本轮是纯文档与协作流程整理，无代码、接口、数据结构或部署行为变化，按 8.1 节第 2 条走三段式补丁号 `v2.1.1`。**本轮的提交、推送与打标签由执行 agent 完成**——这正是本节要确立的规则本身。

## 2026-08-17 修复接入 Cloudflare 后限流按边缘节点分桶：Nginx 层还原真实客户端 IP

- **修复的是一个线上已存在、但不报错也不崩溃的功能缺陷。** 链路为 访客 → Cloudflare → Nginx → Express：Cloudflare 会向源站发 `X-Forwarded-For: <访客IP>` 与 `CF-Connecting-IP: <访客IP>`，但 Nginx 的 `$remote_addr` 是 **Cloudflare 边缘 IP**（实证：日志中出现 `client: 162.158.114.222`，落在 CF 的 `162.158.0.0/15` 段内），`$proxy_add_x_forwarded_for` 追加后 XFF 变为 `<访客IP>, <CF边缘IP>`，而后台 `TRUST_PROXY_HOPS=1` 只跳过最右一跳，`req.ip` 取到的正是 CF 边缘 IP。接 Cloudflare **之前** XFF 只有一项、取最右恰好是访客，所以这是接入之后才悄悄产生的错误。
- **受影响的真实功能共四处**：`src/app.js` 中的四个限流器（密码登录、TOTP、**设备认证 `deviceAuthLimiter`**、留言提交）均未设置自定义 `keyGenerator`，因此全部走 `express-rate-limit` 默认的 `req.ip` 分桶，等同按 CF 边缘节点分桶——一个访客触发限流会连累同边缘节点的其他访客，且 429 文案「该IP的认证尝试过于频繁」中的 IP 实际是边缘地址；第四处是 `feedback_comments.ip_address` 记录的来源 IP（同样记成边缘地址，无取证价值）。设备认证限流是本轮实际核对代码时发现的，原问题描述只列了其中三处。
- **改动仅一处**：`deploy/nginx.conf` 顶部、第一个 `server` 块之前新增 15 条 IPv4 与 7 条 IPv6 共 22 条 Cloudflare 官方网段的 `set_real_ip_from`，加 `real_ip_header CF-Connecting-IP;`。该文件渲染进 `conf.d/default.conf`，位于 `http {}` 上下文内，因此三个 `server` 块（80 跳转、443 主站、443 小作坊）一次生效，无需逐块重复。取 `CF-Connecting-IP` 而非 XFF，是因为前者由 Cloudflare 保证为单个真实访客 IP、不含代理链，无需开启 `real_ip_recursive`。
- **刻意不写 `0.0.0.0/0`，也刻意不改 `TRUST_PROXY_HOPS`。** 本站源站可被直连（实测 `https://<源站IP>/` 返回 200，Cloudflare 不是唯一入口），无条件信任 `CF-Connecting-IP` 等于允许任何人直连源站伪造来源 IP、绕过全部 IP 限流；限定网段后，非 CF 来源发来的该头会被忽略。同理，把 `TRUST_PROXY_HOPS` 由 1 改成 2 虽然也能取到访客 IP，但那是盲信 XFF、同样可被直连伪造，方向错误，明确不采纳。
- 其余指令一律未动，包括 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for`。real_ip 生效后 XFF 会变成 `<访客>, <访客>`，取最右仍是访客；若有人绕过 Cloudflare 直连源站，`set_real_ip_from` 不匹配、`$remote_addr` 保持真实对端，XFF 末位仍是真实来源。两条路径都正确，无需改写。`git diff` 为 63 行纯新增、0 行删除。
- **网段列表需人工维护**：来源为 `https://www.cloudflare.com/ips-v4` 与 `ips-v6`，当前列表取于 2026-08-17。列表过期的失败模式是「新网段来的请求退回记 CF 边缘 IP」——只影响限流精度，属优雅降级、不中断服务，因此刻意不做成启动时联网拉取（那会给容器启动引入网络依赖）。新增内容不含 `${}` 形式变量，无需扩展 `NGINX_ENVSUBST_FILTER` 白名单。
- **本轮只完成部署验证四层中的第 1 层（静态审阅）。** 已实际执行：22 条网段与官方列表逐条程序化比对（顺序一致、无重复、无笔误）、确认全部指令位于第一个 `server` 块之前、确认实证边缘 IP `162.158.114.222` 确实被 `162.158.0.0/15` 覆盖（并有反例对照）、确认新增区块仅在注释中出现 `$remote_addr` 字样而无 `${}` 变量、确认文件行尾仍全为 LF。**未执行**：`nginx -t`、`docker compose config`、容器构建与启动、任何线上验证——本机不跑 Compose/nginx，只跑 `npm start`，因此这些不能由本轮声称完成。
- 待指挥师在服务器完成：拉取、`nginx -t`、重建 nginx 容器，并以日志中的来源 IP 由 Cloudflare 段变为真实访客 IP 作为生效证据，同时复核三个入口仍 200、容器仍 healthy。**仓库改动不等于线上生效。**

## 2026-08-16 v2.0.1 与 v2.0.2 文档补丁存档：协作架构交接与共用域名拓扑

- **补记 v2.0.1（2026-08-15）**：协作架构切换为 Claude Code 担任指挥师，并确立三段式补丁版本号规则（含实质代码/接口/数据结构/部署行为/用户可见功能变化用两段式 `vX.Y`，纯文档或协作流程整理用三段式 `vX.Y.Z`，混合改动按代码变化处理）。该存档当时未追加本文件条目，此处补记以恢复标签与改动记录的连续性。
- **v2.0.2 工作手册**：新增 1.2.1 节「云服务器现场操作：指挥师独占」——Codex 与 Claude Code 执行者均不具备服务器访问能力，SSH 登录后的一切动作由指挥师本人执行，不写进给执行 agent 的指令；受版本控制的部署文件改动仍归执行 agent。4.4 节生产更新流程相应由「交给作为执行者的 Claude Code」改写为「指挥师本人执行」。附 7 条操作红线，其中第 2 条直接引用本项目 `v1.9` 存档时踩到的真实事故：bcrypt 哈希中的 `$` 被 Compose 变量插值截断，导致容器 100% 启动失败。
- **v2.0.2 与知天关系改写**：由「完全独立」更正为——仓库、账号、数据和部署仍完全独立，本仓库不含其业务代码；但两者共用同一台服务器（各占一个公网 IP）与**同一顶层域名**，知天挂在 `agent` 子域名下，本站为首页，后续在 `tools.html` 增加跳转入口。域名分流属跨项目协调，不构成任一方的运行依赖。
- **v2.0.2 新增「与知天的域名与融合边界」一节**：域名归属表（根域与 `www`、`lab` 归本站，`agent` 归知天，`mail` 为阿里云 DirectMail）；**Cloudflare 为唯一生效的 DNS 权威**——NS 已切走，阿里云云解析控制台内的记录不会在互联网生效且其顶部有明确提示，今后改 DNS 只认 Cloudflare；以及融合四层级与各自是否需要改动架构。
- **记录一处安全冲突并升级既有待办**：融合第 3 层「登录状态共享」需把 Cookie 设为 `domain=.zhiliaohub.com`，而 `lab` 子域承载的是管理员上传的**任意网页 ZIP**，其中任意 HTML/JS 将能读到主站与知天的认证 Cookie，等同 XSS 级越权。原「小作坊子域名真实 Cookie 隔离验证」待办因此由「待确认」升级为登录融合的**前置必须项**。另注 Cloudflare 免费通配证书只覆盖一级子域名，新增子域名不要套多层。

## 2026-08-14 v2.0 存档：deployment字段修正与全项目文档校准

- 将 deployment 环境判断、指挥师手册重写和全项目文档校准作为 `v2.0` 存档并推送到主仓库 `main`，创建同名带注释标签“deployment字段修正与全项目文档校准”。
- `/health` 复用现有 `config.isProduction`，依据 `NODE_ENV` 返回 `production` 或 `local-only`，未新增环境变量；开发与生产分支均有自动化断言，后台 `node --check` 与72项测试全部通过。
- `docs/claude_skill.md` 不再沿用早期纯静态站假设，现已覆盖后台规范、双仓库边界和分层部署验证；其余当前状态文档统一为主站 v2.0、配套 App v0.3 与真实HTTPS生产部署。
- 本次Git存档不等于生产部署：服务器仍需由用户手动拉取 v2.0、显式重建并重启 `admin-server`，再确认线上 `/health` 返回 `deployment: production`。真实异地备份、`lab-storage`备份、小作坊子域名Cookie隔离验收和后台测试接入CI仍是待办。

## 2026-08-14 生产环境标识修正与全项目文档校准

- `/health` 的 `deployment` 不再沿用 v1.1 本地开发阶段的永久硬编码：复用现有 `NODE_ENV` / `config.isProduction`，生产环境返回 `production`，开发和测试继续返回 `local-only`，其余健康响应字段与状态码不变；新增双环境自动化测试。
- 校准 `claude_memory.md`、`zhiliaohub_structure.md`、根 `README.md` 与 `admin-server/README.md`，统一主站 v1.9、配套 App v0.3、`https://zhiliaohub.com` 已真实上线，以及当前认证、内容管理、反馈、小作坊和备份能力。
- 将 `admin-server/deploy/README.md` 从部署前片段说明重写为当前生产拓扑、日常代码更新、首次部署/迁移/灾难重建与待验证边界的运维手册；继续禁止把真实IP、路径、证书和密钥写入仓库。
- 本轮没有连接或修改生产服务器；线上容器在代码提交、推送并由用户显式拉取、重建和重启前仍会返回旧的 `deployment: local-only`，必须在服务器更新后再次读取 `/health` 验证。

## 2026-08-14 指挥师工作手册全面审计重写（仅文档）

- 基于主站 v1.9、配套 App v0.3、当前代码结构、完整改动历史与真实线上入口，全面重写 `docs/claude_skill.md`；保留“指挥师不写代码、不单方面改变架构、不把设想写成现状、提交需用户确认”等核心原则。
- 将协作边界从早期“纯静态单仓库”更新为静态前台、Express/SQLite/Markdown后台、保存即发布、认证与反馈、备份恢复、Docker/Nginx/HTTPS部署及独立 Android App 两仓库模式，并明确两仓库独立状态、提交、标签和兼容性维护规则。
- 新增后台编码、安全隐私、文件职责和分层验证规范：后台要求 `npm run check` 与 `npm test`，部署区分静态审阅、配置解析、本地容器和真实线上验证，CI覆盖范围不得被夸大。
- 本轮只修改协作手册与改动记录，不涉及功能、数据库、部署配置或线上服务变更；审计发现的其他过期文档口径留待用户确认后单独校准。

## 2026-08-13 v1.9 存档：正式部署配置文件整理（当时未执行部署）

- 将此前只存在于 `admin-server/deploy/README.md` 的Compose/Nginx设计片段整理为仓库根目录正式 `docker-compose.yml` 与 `deploy/nginx.conf`。Compose构建 `admin-server`，使用官方 `nginx:stable-alpine`，挂载SQLite、Markdown、上传、备份、小作坊五个后台持久目录，以及后台可写/Nginx只读的独立静态站点目录。
- 新增根目录 `.env.example`，仅承载Compose拓扑变量：监听IP和端口、主站/小作坊主机名、TLS宿主路径、镜像标签与持久目录路径；根目录真实 `.env` 已加入 `.gitignore`，与保存后台密钥的 `admin-server/.env` 明确分层，二者不可混用。
- Nginx正式配置启用80到HTTPS跳转，443下主站直接伺服静态文件，并把 `/admin`、`/api`、`/uploads`、`/health` 反代到后台；完整转发真实IP与协议请求头，对应后台强制 `TRUST_PROXY_HOPS=1`。小作坊使用独立主机名和只读目录，不反代到Node，并保留受限CSP。
- 为防止错误暴露仓库，部署要求 `SITE_ROOT_PATH` 指向只含公开前台文件的独立目录，禁止把整个Git检出目录挂给Nginx；同时补齐 `.dockerignore` 对真实 `lab-storage` 内容的排除，避免其进入Docker构建上下文。
- **修复一个会导致容器100%启动失败的配置缺陷。** 存档前真实执行 `docker compose config` 时发现：Compose 会对 `env_file` 的值做变量插值，而 bcrypt 哈希形如 `$2b$12$<盐与哈希>`，其中的 `$` 被当成变量引用，`$2b`、`$12`、`$<盐与哈希>` 三段均未定义而被替换为空，最终传入容器的 `ADMIN_PASSWORD_HASH` 只剩 `$2b$12`。后台启动时校验不通过、抛出 `ADMIN_PASSWORD_HASH must be a bcrypt hash.` 并直接退出，也就是说按修复前的配置部署，容器必然起不来。已改为 `env_file: - path: ./admin-server/.env` 配合 `format: raw` 关闭该文件的插值；任何含 `$` 的密钥（例如自定义的备份加密密码）都同样受此保护。已用合成哈希复现问题并验证修复，修复后 `docker compose config` 退出码为0且不再有"变量未设置"告警。
- 配置未写入任何真实IP、域名、证书路径或凭据。**已实际执行**的验证：`docker compose config` 通过（本机有 Docker CLI 29.6.2 与 Compose v5.3.1，该命令为客户端侧解析，不需要守护进程），变量插值、六个绑定挂载、端口发布与两个服务的依赖关系均按预期解析。**未执行**的验证：本机 Docker 守护进程当前未运行，因此 `nginx -t`、镜像构建、容器启动、端到端联调均未进行；宿主机也没有安装 Nginx。Nginx 配置只完成逐项人工审阅，真实TLS、来源IP透传及小作坊Cookie隔离仍需在服务器上验证。
- 需要在部署时人工确认的一点：主站与小作坊两个 `server` 块共用同一份 `fullchain.pem`。若 `SERVER_NAME` 与 `LAB_SERVER_NAME` 属于不同主机名，该证书必须同时覆盖两者（SAN 或通配符），否则小作坊站点会出现证书不匹配。

## 2026-08-13 v1.8 存档：备份/恢复体系加固

- 将备份/恢复体系的两轮加固作为单一 `v1.8` 提交 `836fc62` 推送到 [GitHub仓库](https://github.com/z987645344-arch/zhiliaohub) 的 `main` 分支，并创建同名带注释标签 `v1.8`（标签信息："备份/恢复体系加固：定时触发、异地模拟接口与文档重写"）。标签已确认存在于远程，本地 `main` 与 `origin/main` 同步。
- 本次存档共13个文件、1054行新增与24行删除，其中3个为新增文件：`src/services/backup-scheduler.js`（进程内定时备份）、`src/services/backup-destination.js`（可插拔备份目的地接口）、`tests/backup-automation.test.js`（8项新测试）。其余为 `backup-service.js`、`backup-config.js`、`server.js`、两个CLI脚本、`backup.test.js`、`README.md`、`.env.example`、`CHANGELOG.md` 与 `docs/claude_memory.md`。
- **本次提交包含两轮工作**：除本轮的四部分外，还一并收录了上一轮完成但尚未提交的"恢复前自动快照"功能（`createPreRestoreSnapshot` 在 `v1.7` 中并不存在）。提交正文中已单列该项，避免日后回看时漏掉这次提交里最核心的功能。
- 存档前确认：工作区无未跟踪遗留；真实 `.env`、开发数据库 `admin.sqlite3` 与 `backups/` 下的真实归档均由 `.gitignore` 排除且未进入提交；提交diff中不含任何凭据、bcrypt哈希、session/TOTP密钥、本地临时目录路径或测试残留；新增文件中不含字面NUL字节；`npm run check` 与后台71项测试全部通过。
- CI结果：`Static checks` 工作流针对提交 `836fc6204da73128beb6dc2bd62f5f9a54e9a7fb` 运行 [#31676075063](https://github.com/z987645344-arch/zhiliaohub/actions/runs/31676075063) 成功通过，用时12秒。该CI仍只做JavaScript语法与HTML本地引用的静态检查，**不会**安装后端依赖、不会执行71项测试、也不会验证定时备份或异地同步；本轮全部功能验证均在本地隔离环境真实执行，CI通过不等于这些能力在CI环境被重新验证过。
- 版本边界不变：`v1.8` 只是让备份/恢复机制本身更可靠，**没有**解决服务器整机损毁的风险。`BACKUP_MIRROR_DIR` 仍只能指向同一台机器上的另一个目录，属于本地模拟而非真正异地容灾；真实远程存储接入、备份失败告警、服务器级恢复演练以及把 `lab-storage/` 纳入备份范围，都仍是待办。

## 2026-08-11 备份体系收尾：细节核实、定时触发、异地接口与面向陌生人的恢复文档

### 一、两个遗留细节的核实结果

- **`--skip-pre-restore-snapshot` 能否单独使用：核实后确认不能，无需改动。** `scripts/restore.js` 在解析参数后就有 `if (!archive || !force)` 拦截，缺少 `--force` 时只打印用法并以状态码1退出，根本不会调用恢复；服务层 `restoreBackup()` 第一行同样有 `if (!options.force) throw`。已实测三种情况：只带 `--skip-pre-restore-snapshot`、不带任何开关、以及绕过CLI直接调用服务层，分别被CLI和服务层拒绝。也就是说跳过安全网**本来就必须同时带上 `--force` 和 `--skip-pre-restore-snapshot`**，用户希望的双重确认已经存在，本轮没有为此增加冗余代码。
- **"没有数据库"的判断：核实后确认原实现把两种情况混为一谈，已修正。** 原代码是 `await fs.stat(config.databasePath).then((stat) => stat.isFile()).catch(() => false)`，`.catch(() => false)` 会吞掉**所有**错误：文件确实不存在（ENOENT）与文件存在但读不出来（EACCES/EPERM/EIO 等）得到完全相同的结果，后者会被静默当作"全新机器"跳过快照并继续覆盖。新增 `inspectLiveDatabase()` 只把 ENOENT 视为"确实没有数据库"，其余错误一律抛出明确错误并中止恢复；路径存在但不是普通文件（例如是目录）也单独报错。错误文案明确提示"当前数据库本身可能已经损坏，需要人工确认后再覆盖"。

### 二、进程内定时备份

- 新增 `src/services/backup-scheduler.js`，在后台服务进程内用 `setInterval` 定时触发，复用现有 `createBackup` 而非另写一套。刻意不用操作系统级 cron：容器化部署时宿主机的 cron 不会随镜像分发，进程内实现在本地和容器中行为一致。
- "上次备份是什么时候"直接从备份目录里最新的 `backup-<时间戳>` 文件名解析，不额外维护状态文件。因此重启安全是**结构上自带**的：状态本来就在磁盘上，重启后立刻可读；刚备份完就重启不会重复触发，停机很久后重启会在启动时立即补一次。
- 可配置 `BACKUP_SCHEDULE_ENABLED`（默认启用）、`BACKUP_INTERVAL_MS`（默认24小时）、`BACKUP_SCHEDULE_CHECK_INTERVAL_MS`（默认15分钟）。已接入 `server.js` 启动与优雅关闭。
- 失败绝不静默：`tick()` 不向定时器回调抛出，而是记录 `[backup]` 前缀的错误日志，写明失败原因、连续失败次数，并明确提示"现在没有产生新的备份，可恢复的最新数据仍停留在上一次成功备份的时间点"。
- 修复了一处自己引入的日志缺陷：启动日志原本固定按小时取整，短间隔时会打印出无意义的"每 0 小时一次"，已改为按时长自动选择小时/分钟/秒/毫秒单位。

### 三、备份目的地抽象（本地模拟，非真实异地）

- 新增 `src/services/backup-destination.js`，定义 `name` / `describe()` / `send(archivePath)` / 可选 `prune()` 的目的地契约，并实现唯一的 `LocalMirrorDestination`：把归档复制到另一个本地目录。复制先写临时文件再重命名，避免中断留下看起来可用的半截文件。
- **必须明确：这是本地模拟，不是真正的异地容灾。** 副本与主备份在同一台机器上，服务器整机损毁、磁盘报废时两者会一起消失。本轮只验证了"备份生成后自动同步到第二个位置"这条链路本身成立。真正解决单点故障必须接入真实远程存储（如腾讯云COS），**该部分尚未实现、尚未验证**，接口已预留，将来只需实现同一契约、不必改动调用方。
- 同步失败绝不牵连本地备份：`replicateArchive()` 捕获全部异常、只记录日志并返回失败状态，不重新抛出、不删除也不改写已经生成好的本地归档。

### 四、恢复文档改写

- 重写 `admin-server/README.md` 的"备份与恢复"整章，读者假设为多年后完全不记得项目细节的本人：开头是"只想恢复到最近一次正常状态"的三步极简流程（含预期输出长什么样），随后是名词对照表（后台/SQLite/Markdown正文/归档/恢复/恢复前快照分别是什么）、备份可能存在的几个位置、为什么必须先停服务、失败信息逐条对照的排查表，最后才是选择历史版本、强制恢复、定时备份、加密和已知边界等进阶内容。时间戳含义（UTC、比北京时间早8小时、按文件名排序即可）也写明了。

### 验证结果

- 真实定时器验证（非手工调用 `tick()`）：把间隔压到1秒、检查压到200毫秒后启动真实调度器，3.5秒内真实产生4份归档；随后模拟重启确认未重复，等待超过一个间隔后重启确认立即补备份；每份归档都被复制到模拟异地目录。12项核对全部通过。
- 端到端可用性：删除**整个本地备份目录**后，仅凭模拟异地目录里的副本完成了一次完整恢复，作品标题与Markdown正文内容核对一致；定时备份产生的归档也能被现有恢复流程正常识别使用。
- 故障隔离：把模拟异地目标改成不可写后，备份本身仍然成功、失败被如实记录，且该本地归档随后仍能完成一次真实恢复。
- 服务集成：用隔离环境变量真实启动 `node src/server.js`，日志确认调度器随服务启动、按期产生备份并完成同步，关闭后进程与临时目录一并清理。
- 文档核对：README 中写出的每条命令与预期输出都在隔离环境中真实执行过，包括 `--force --skip-pre-restore-snapshot` 组合确实跳过快照且不产生 `pre-restore-` 文件。
- 测试从63项增加到71项，新增8项覆盖上述场景；`npm run check` 与全部71项测试通过。
- 过程中发现自己写的一处测试可读性缺陷：用于触发"非 ENOENT 错误"的路径里嵌入了**字面 NUL 字节**，在编辑器和 Read 工具里都显示成普通空格，导致我自己复核时按空格重写而得出相反结论。该断言本身是正确的（NUL 会让 `fs.stat` 抛出 `ERR_INVALID_ARG_VALUE` 而非 ENOENT），但已改为 `String.fromCharCode(0)` 显式构造并加注释说明，避免以后再次误读。
- 全部验证在系统临时目录中进行并已删除；真实 `.env`、真实开发数据库与 `admin-server/backups/` 未被读写或产生测试文件。

## 2026-08-11 恢复前自动快照：核实缺口并补齐恢复安全网

- **核实结果：现有代码原本确实没有这层保护。** 通读 `scripts/restore.js`（只是薄CLI包装）与 `src/services/backup-service.js` 的 `restoreBackup()` 后确认，原实现在归档校验通过后直接进入覆盖：先 `atomicWriteFile` 覆盖SQLite并删除 `-wal`/`-shm`，再依次 `replaceDirectory` 替换 `content/works`、`content/notes` 和 `uploads`，整条恢复路径没有任何 `createBackup` 调用；改动前 `createBackup` 只被 `scripts/backup.js` 和测试引用。这不是推测，是读完全部实现后的结论。
- 阅读时还确认了两个相关暴露面：`replaceDirectory` 虽有单目录回滚，但回滚副本在该目录成功后即被删除，只能覆盖"这一个目录自身操作失败"，因此 `notes` 失败时先前已成功的 `works` 不会回滚；而SQLite是最先被覆盖的，此前完全没有任何保护。两者都由本轮的恢复前快照兜底。
- 新增恢复前快照：在归档通过 manifest 与 SHA-256 校验之后、第一次写入之前，自动把当前的SQLite、Markdown与上传文件另存为 `pre-restore-<时间戳>.tar.gz`。实现直接复用未经修改的 `createBackup`，因此快照与常规备份具有完全相同的清单、校验和与可选加密，可以直接当作普通归档再次恢复。
- 保留策略采用独立池：`createBackup` 新增 `namePrefix` 参数，`pruneBackups` 按前缀生成正则。常规备份仍匹配 `backup-` 前缀并遵守 `BACKUP_RETENTION_COUNT`，恢复前快照匹配 `pre-restore-` 前缀并遵守新增的 `PRE_RESTORE_RETENTION_COUNT`（默认3）。两者互不清理，常规备份的"最近N份"永远不会挤掉刚生成的快照。
- 失败即中止：快照创建失败时抛出明确错误并终止恢复，不执行任何覆盖性写入。唯一例外是目标环境尚不存在数据库（全新机器首次恢复），此时本来就没有需要保护的数据，会跳过快照并在CLI输出中明确告知"本次恢复没有回退点"。
- 另外新增了显式的 `--skip-pre-restore-snapshot` 开关。这一项超出了字面要求，理由是：如果当前数据库已损坏到 `createBackup` 无法读取，而快照失败一律中止，恢复工具在最需要它的灾难场景下反而完全不可用。默认行为仍是中止，必须由操作者主动加参数才会在无安全网的情况下恢复。
- **真实验证（不是只读代码）**：在完全隔离的临时目录中构造"旧归档 → 当前最新数据 → 误选旧归档恢复 → 用恢复前快照退回"的完整场景，19项核对全部通过。不仅确认快照被创建，还确认误恢复真的把数据退回了旧状态（作品标题变回旧值、只存在于最新状态的日记消失、上传文件内容变回旧内容），再确认用快照恢复后作品标题与摘要、两份Markdown正文、两个上传文件内容与恢复前逐字节一致。另在CLI层用环境变量覆盖路径隔离运行了真实的 `node scripts/backup.js` 与 `node scripts/restore.js`，完成同一条误选与找回链路，全程没有读写真实 `.env` 与真实开发数据库。
- 中止行为同样真实验证：注入不可用的备份目标后，恢复以"Restore aborted: the pre-restore snapshot could not be created"拒绝，且SQLite记录、Markdown正文与上传文件全部保持原样。该测试第一版写错过——破坏备份目录时连同要恢复的归档一起破坏了，导致失败其实发生在读取归档阶段而非快照阶段；已改为把归档放到备份目录之外，确保中止确实由快照失败触发。
- 测试从59项增加到63项，新增4项覆盖：误选归档后经快照找回、快照失败中止且数据不变、独立保留池、无数据库时跳过快照。`npm run check` 与全部63项测试通过。
- 清理与边界：全部验证在系统临时目录中进行，结束后已删除；真实开发数据库与 `admin-server/backups/` 未产生任何测试文件。恢复前快照与常规备份写在同一个 `BACKUP_DIR`，整个目录或整块磁盘损坏时两者会一起丢失，该场景仍需异地副本，本轮未涉及。

## 2026-08-11 v1.7 存档：Docker镜像真实构建验证与部署前提修复

- 将首次真实Docker构建验证与随后的收尾修复作为单一 `v1.7` 提交 `427271d` 推送到 [GitHub仓库](https://github.com/z987645344-arch/zhiliaohub) 的 `main` 分支，并创建同名带注释标签 `v1.7`（标签信息："Docker镜像真实构建验证与部署前提修复"）。标签已确认存在于远程，本地 `main` 与 `origin/main` 同步。
- 本次存档共6个文件、65行新增与15行删除：`admin-server/Dockerfile`（npm ci 原生模块修复与非root目录权限修复）、`admin-server/src/config.js`（SITE_ROOT 环境变量支持）、`admin-server/deploy/README.md`（lab-storage 与静态前台目录挂载、TRUST_PROXY_HOPS 必需项说明）、`admin-server/.env.example`（SITE_ROOT 注释）、`CHANGELOG.md` 与 `docs/claude_memory.md`。
- 存档前确认：工作区无未跟踪文件；提交diff中不含任何测试用凭据、bcrypt哈希、session/TOTP密钥、本地临时目录路径、测试容器名或镜像摘要；`npm run check` 与后台59项测试均通过。
- CI结果：`Static checks` 工作流针对提交 `427271de2526119ab68e202be9acc9ed8a058b2b` 运行 [#31488609749](https://github.com/z987645344-arch/zhiliaohub/actions/runs/31488609749) 成功通过，用时17秒。该CI只做JavaScript语法与HTML本地引用的静态检查，**不会**安装后端依赖、不会构建Docker镜像、也不会执行本轮的容器验证；本轮的构建与容器验证全部在本地真实执行，CI通过不等于镜像在CI环境被重新验证过。
- 版本边界不变：`v1.7` 只包含可构建可运行的镜像与完整的部署片段，仍未连接真实服务器、域名、HTTPS 或 nginx。`<SITE_ROOT_PATH>` 与 nginx 站点根目录是否为同一目录、服务器目录属主与镜像内UID是否匹配，都必须在实际部署时人工核对；备份脚本仍未覆盖 `lab-storage` 数据面。

## 2026-08-11 Docker收尾：SITE_ROOT可配置与lab-storage持久化

- 修复上一轮真实构建验证中记录的两个遗留问题，改动范围限定在 `config.js` 一行、`.env.example` 注释和 `deploy/README.md` 部署片段，没有附带其他调整。
- **`SITE_ROOT` 环境变量覆盖**：`config.js` 中 `siteRoot` 改为 `resolveLocalPath(process.env.SITE_ROOT, '..')`，与 `DATA_DIR`、`CONTENT_DIR`、`LAB_STORAGE_DIR` 等既有路径覆盖写法保持一致。未设置时仍解析为 `admin-server/..` 即仓库根目录，已实测与改动前完全相同，本地开发行为不受影响；`.env.example` 增加了对应的注释说明。
- **部署片段补全**：Compose 片段新增 `SITE_ROOT: /app/site` 环境变量、`<SITE_ROOT_PATH>:/app/site` 与 `<ADMIN_LAB_STORAGE_PATH>:/app/lab-storage` 两个卷挂载；占位符清单由四个持久目录改为五个并新增静态前台目录说明；持久化说明写明 `lab-storage` 必须挂载且**不在**现有备份三个数据面之内；人工部署顺序第1步和第5步补充了站点目录属主核对与发布/重启后的实测项。
- **静态发布已在容器内真实验证**：容器设置 `SITE_ROOT=/app/site` 并挂载一个独立的前台目录副本（不是仓库本体，避免污染工作区），通过真实API走完密码登录、TOTP绑定、`POST /admin/notes` 创建日记并触发全量发布，返回 `302 /admin?notice=日记已保存并发布。`。随后在**容器外**的宿主机目录上逐项核对内容，12项检查全部通过：`notes.html` 含新日记标题与摘要且带“自动生成”标记、生成了对应的日记详情页且Markdown正文已渲染为HTML、`works.html` 与 `feedback.html` 同步重新生成、手工维护的 `index.html` 校验和保持不变未被发布覆盖、`css/style.css` 与 `js/site.js` 仍在。共7个文件被真实写入挂载目录。
- 验证过程中确认了一个部署前提：`NODE_ENV=production` 会使session cookie带上 `secure`，纯HTTP下 express-session 不会下发cookie、登录必然失败。因此 Compose 片段里的 `TRUST_PROXY_HOPS: 1` 是必需项而非可选项，必须由nginx终止TLS并转发 `X-Forwarded-Proto`。本轮验证即按该拓扑模拟完成，没有为了跑通而降低生产配置。
- **lab-storage持久化已真实验证**：通过 `/api/admin/lab/upload` 真实上传ZIP并解压（返回201），随后不仅做了普通重启，还做了**完整重建**（`docker rm` 后从镜像重新 `docker run`）——新容器内文件仍在、`/lab/<slug>/` 返回200且页面标记内容正确、数据库记录同样保留。反向对照也做了：按旧配置重建（不挂载 `lab-storage`）时数据库仍列出该项目但目录为空、`/lab/<slug>/` 返回404，证明确实会产生记录与文件不一致的数据丢失，也证明本轮新增的挂载正是解决该问题的原因。
- 回归：改动后本地59项测试全部通过，容器内59项测试同样全部通过（退出码0）。
- 清理与边界：测试容器已全部删除，临时的前台副本、lab-storage、数据目录、测试ZIP与脚本已删除，无残留容器和悬空卷。本地开发数据库 `admin.sqlite3` 修改时间早于本轮全部容器操作，仓库内前台HTML/CSS/JS没有被发布写入过。本轮仍**没有**真实服务器、域名、HTTPS与nginx合并，`<SITE_ROOT_PATH>` 与 nginx 站点根是否为同一目录、真实服务器上的目录属主是否匹配镜像内UID，都只能在实际部署时核对。

## 2026-08-11 首次真实Docker构建与容器运行验证

- 这是 `admin-server/Dockerfile` 第一次被真正执行 `docker build`。此前该文件只经过静态代码审阅，从未构建过镜像。本轮在 Docker 29.6.2（Docker Desktop、linux/x86_64）上完成真实构建、启动容器并验证。
- **首次构建失败**：`npm ci` 对 `better-sqlite3` 触发隐式 `node-gyp rebuild`，而 `node:22-bookworm-slim` 不含 Python 与编译器，报错 `Could not find any Python installation to use`。定位结论：`better-sqlite3@13.0.2` 自带 `prebuilds/linux-x64.node` 预编译产物并声明 `"gypfile": false`，但其包根目录仍保留 `binding.gyp`；`npm install` 会遵守 `gypfile:false`，`npm ci` 不遵守而强行走 node-gyp。使用仓库现有 lock 文件与容器内重新生成的 lock 文件分别复现，结果一致，因此与 lock 文件是否过期无关。
- **为构建成功所做的改动（仅限 `admin-server/Dockerfile`，共两处）**：
  1. `npm ci` 改为 `npm ci --ignore-scripts`，`npm prune --omit=dev` 同步加 `--ignore-scripts`，直接使用官方发布的预编译二进制，不在镜像内引入 Python/g++ 工具链。已验证 `better-sqlite3` 与 `bcrypt`（经 `node-gyp-build` 解析自带预编译）在容器内均可正常加载和执行。
  2. 新增 `RUN mkdir -p data lab-storage && chown node:node data lab-storage`。此前容器启动即崩溃，报 `EACCES: permission denied, mkdir '/app/data'`：`WORKDIR /app` 由 root 创建且属主为 `root:root`，非root的 `node` 用户无法在其中创建 `initializeDatabase()` 启动时所需的 `data/` 与 `lab-storage/`。`/app` 本身保持 root 属主，运行用户无法改写应用代码。
- **非root已真实生效**：容器内 `whoami` 为 `node`，`/proc/1/status` 显示 1 号进程（`node src/server.js`）`Uid: 1000`，宿主机 `docker top` 同样显示 UID 1000。写入探测确认 `/app` 对 `node` 不可写、`/app/data` 与 `/app/lab-storage` 可写。
- **/health 正常**：宿主机 `http://127.0.0.1:13001/health` 与容器内 `127.0.0.1:3001/health` 均返回 `200` 及 `{"status":"ok","storage":"sqlite+markdown","deployment":"local-only"}`；Docker 内建 HEALTHCHECK 状态为 `healthy`。
- **数据库读写已真实验证**：在容器内运行现有 59 项测试套件（`tests/` 被 `.dockerignore` 排除，以只读方式挂载）。裸镜像下 51 项通过、8 项失败，8 项全部为镜像刻意不包含的文件导致的 `ENOENT`——6 项因 `data/schema.sql` 已被 Dockerfile 移到 `/app/schema/schema.sql`（这些用例未传 `schemaPath`，回落到 `serverRoot/data/schema.sql`），2 项读取的是静态前台的 `/index.html` 与 `/js/site.js`。把这些路径只读补齐后，**59 项全部通过、退出码 0**，没有任何一项失败与 SQLite 或原生模块相关。
- 另在未挂载任何内容的原始容器上完成一次端到端真实写入：`POST /api/feedback/comments` 返回 `202`，随后在容器内直接查询 `/app/data/admin.sqlite3`，记录数由 0 变为 1、`status` 为 `pending`，中文字段按 UTF-8 逐字节比对完全一致，`admin.sqlite3-wal` 与 `-shm` 均已生成，确认是真实落盘写入而非内存假象。
- **本轮遗留问题（未修复，超出"让构建/运行成功"范围）**：`config.js` 中 `siteRoot = path.resolve(serverRoot, '..')`，在容器内解析为根目录 `/`，而 `/` 对非root用户不可写，因此容器内执行全量静态发布会失败。修复需要为 `siteRoot` 增加独立环境变量并挂载前台目录，属于部署方案设计，应单独讨论后再改。此外 `admin-server/deploy/README.md` 的 Compose 片段只挂载了 `data`、`content`、`uploads`、`backups` 四个卷，未挂载 `lab-storage`，小作坊数据会在容器重建时丢失，也未纳入备份范围。
- 验证完成后已停止并删除测试容器，未残留知了hub相关容器或悬空卷；构建产物镜像 `zhiliaohub-admin:test`（387MB）保留备查。本地开发环境未被污染：`admin-server/data/admin.sqlite3` 等文件修改时间早于本轮容器操作，`git status` 仅显示 `admin-server/Dockerfile` 一处改动。
- 需要明确的边界：本轮只验证了镜像可构建、容器可以非root启动、健康检查通过和SQLite可真实读写；仍**没有**真实服务器、域名、HTTPS、反向代理合并、生产数据卷与权限核对，也没有做任何真实部署。

## 2026-08-11 v1.6 四轮功能存档

- 将反馈评论系统、作品分组展示、五项导航与智能工具占位页、小作坊四轮改动作为单一 `v1.6` 提交推送到 [GitHub仓库](https://github.com/z987645344-arch/zhiliaohub) 的 `main` 分支，并创建同名带注释标签。
- 反馈评论采用公开提交、蜜罐与IP限流、SQLite先审后发、后台论坛式审核和approved-only静态发布；邮箱与IP永不进入前台产出。
- 作品一级页按程序/影视/生活展示最新内容并支持触摸、鼠标拖动、滚轮和箭头横向浏览；三个固定二级分类页展示全部作品。全站统一五项导航，新增不含登录、注册或API能力的智能工具说明页。
- 小作坊支持受保护的ZIP上传、安全解压、静态访问、显隐与删除；路径穿越、解压炸弹、非法类型、符号链接和加密条目均会拒绝。作品页仅在存在可见项目时生成小作坊区块。
- 存档前确认仓库包含19个HTML与42个JavaScript文件；后台59项测试、全部JavaScript语法检查、19个HTML本地引用检查及生产依赖审计均通过。
- 本版本仍未真实部署；小作坊真实子域名隔离和反馈系统真实访客使用场景需部署后验证。8个作品与3篇日记仍为明确标注的占位内容，智能工具真实集成仍待单独讨论。

## 2026-08-11 小作坊：受控ZIP发布与本地静态访问

- SQLite新增 `lab_projects`，后台新增“小作坊管理”页及受管理员会话、CSRF保护的上传/显示切换/删除接口。上传ZIP经校验后解压到独立 `lab-storage/<slug>/`，生成可配置 `LAB_BASE_URL` 下的静态访问链接；删除项目会同步清理记录和目录。
- 解压前扫描完整目录：拒绝绝对路径、盘符、`..` 路径穿越、符号链接、加密条目、非网页资源扩展名和缺少根目录 `index.html` 的包；默认最多500个条目、总解压后大小100MiB，并同时核对声明大小与实际流式解压字节数，防止zip-slip与常见zip炸弹。ZIP解析采用 `unzipper`，生产依赖审计为0项漏洞。
- `/lab/<slug>/` 在session中间件之前提供只读静态响应，设置禁止跨域连接、表单提交、框架嵌入和对象加载的CSP，并不产生 `Set-Cookie`。localhost同源下浏览器仍可能主动发送已有Cookie，但路由不会读取它；真实子域名的Cookie隔离需部署后用真实DNS与HTTPS验证。
- 全量发布会查询 `is_visible=1` 的项目，在 `works.html` 底部按需生成“小作坊”区块；没有可见项目时整个区块省略。显示切换和删除会触发全量发布，访问链接在新窗口打开。
- 自动化新增4项小作坊安全与集成测试，覆盖真实multipart上传、认证/CSRF、正常解压、路径穿越、超限解压、PHP拒绝、显示切换、删除清理及静态响应头。真实浏览器检查后台、前台和解压站点的HTML/CSS/JS/图片，桌面与390px均无横向溢出或控制台错误。
- 新增 `admin-server/deploy/lab-subdomain.md`，准备未来 `lab.zhiliaohub.com` 独立Nginx静态server块和环境变量说明；本轮只在localhost验证，域名解析、证书及真正不共享Cookie的子域名隔离均如实保留为部署阶段待办。

## 2026-08-11 五项全站导航与智能工具占位页

- 全站主导航统一为“首页 / 作品展示 / 学习心得 / 智能工具 / 反馈中心”，编号依次为01至05；手写首页和共享发布模板同步更新，作品一级/二级/详情、日记列表/详情及反馈生成页重新全量发布后均使用相同顺序和正确 `aria-current="page"`。
- 新增手写静态页 `tools.html`，复用现有水泥灰/雾蓝灰视觉、页头、内容区块、有效路由卡片和页脚。页面明确说明未来可能关联知天工具与账号系统，但当前仅处于建设/规划阶段、不承诺上线时间，也没有表单、输入框、登录、注册、API调用或伪功能按钮。
- 智能工具入口与“知天”作品条目保持独立：工具页唯一内容链接指向现有 `works-zhitian.html`；知天仍是程序类作品，继续显示“展示入口待开放”，本轮未修改其数据、详情内容或认证边界。
- 自动化回归55项、40个仓库JavaScript文件语法检查、19个前台HTML本地引用检查全部通过。真实浏览器逐类检查首页、作品一级/二级/详情、日记列表/详情、反馈和工具页；桌面与390px均为五项同序导航、当前项正确、无横向溢出，移动汉堡菜单完整可用，控制台无错误。

## 2026-08-11 作品展示一级分组与二级分类页

- `works.html` 改为“程序 / 影视 / 生活”三个固定分组；每组按 `updated_at DESC, created_at DESC, id DESC` 选择最多4个最新作品。真实SQLite当前分布为程序4、影视4、生活0，空的生活分组显示明确文案，不虚构占位作品。
- 每个有内容的分组使用原生CSS `scroll-snap` 横向容器，并由共享JavaScript补充左右箭头、首尾禁用状态、桌面鼠标拖动和滚轮横向浏览；宽屏4张卡片正好铺满时箭头自动禁用，较窄视口内容溢出时才启用。
- 新增固定二级页 `works-category-program.html`、`works-category-film.html`、`works-category-life.html`，分别展示该分类全部真实作品，复用现有卡片、水泥灰/雾蓝灰视觉、导航和页脚；二级页可返回一级页并继续进入原有Steam风格详情页。
- 发布服务把三个二级页纳入保存即全量发布、写前快照、失败回滚和带标记旧页清理；一次性内容迁移脚本同步改用现行三分类，避免新建库导入旧分类后分组页为空。`index.html`、`notes.html`、`feedback.html`内容未改变，作品详情模板未改变。
- 自动化回归54项、40个仓库JavaScript文件语法检查、18个前台HTML本地引用检查全部通过。真实浏览器验证1280px、900px和390px：箭头、鼠标拖动、滚轮横移、首尾禁用、二级页/详情/返回链路均正常，三个二级页分别显示4/4/0条，页面无横向溢出且控制台无错误。

## 2026-08-08 反馈评论系统三阶段计划全部完成：公开提交、先审后发与静态展示

- 新增反馈静态模板并把 `feedback.html` 纳入全量发布、文件快照和失败回滚范围。发布查询在SQL层只选择 `status='approved'`，且选择列只包含公开所需的ID、父级、作者名、正文、时间和管理员回复标记；pending/rejected、邮箱和IP不会被读取到模板输入。
- 已批准顶层留言及其已批准二层回复按论坛式结构写入静态页，普通回复保持缩进，`is_admin_reply=1` 显示“站长回复”标签；没有已批准内容时显示真实空状态。原占位评论、“暂未开放”和重复评论表单已全部移除。
- 公开留言和顶层留言下方的回复表单调用既有 `POST /api/feedback/comments`；保留称呼、可选邮箱、正文和视觉隐藏蜜罐。邮箱文案明确仅供站长私下联系且永不公开；二层回复没有继续回复入口，服务端仍执行同样的两层深度校验。
- 前端对所有202响应（包括蜜罐静默接受）统一显示“留言已提交，正在等待审核”，不会暗示已公开；字段错误、IP限流、HTTP异常和网络不可达分别显示服务端具体错误或明确的可读提示。页面加载只读取静态HTML，只有访客主动提交时才请求后台。
- 安全端到端验证完成：正常顶层留言和回复均先pending且审核/发布后才出现；rejected正文、三个测试邮箱、字段名 `author_email` 和IP均不在生成源码；蜜罐响应与正常提交文案一致且未写库；第5次窗口内提交触发429并显示精确限流提示；二层回复没有回复按钮，站长回复有明显标签。
- 后台完整回归53项、40个JavaScript文件语法检查和15个前台HTML本地引用检查全部通过。真实浏览器桌面端无横向溢出且页面控制台无警告/错误；同源390px内层视窗实测内容宽375px、无横向溢出。测试容器本身有一条浏览器工具注入的MutationObserver错误，直接页面日志为空，不属于站点脚本。

## 2026-08-08 反馈评论系统三阶段计划第二阶段：后台论坛式审核

- `feedback_comments` 新增 `is_admin_reply` 标记；新建数据库直接采用新结构，既有数据库由 `addMissingColumns` 自动补齐且默认保持普通访客留言语义。
- 反馈服务新增按主题分组查询和待审核计数：顶层留言与其全部二层回复一起展示，“待审核主题”筛选会保留已通过的上下文；新增待审核通过、待审核/已通过内容隐藏，以及固定作者“站长”的管理员直接回复。管理员回复直接为 `approved`，仍只允许回复已通过的顶层留言，不产生第三层嵌套。
- 新增受管理员会话和CSRF保护的 `/admin/feedback` 论坛式审核页及写操作。待审核、已通过、已隐藏使用明显不同的状态标签和背景；邮箱/IP默认折叠，二层回复缩进显示且不提供继续回复入口。管理面板首页同步显示待审核数量。
- 审核、隐藏和站长回复只更新SQLite，不调用发布服务；`feedback.html`、前台模板和静态文件均未修改，前台仍维持阶段一后的占位状态。
- 新增旧库字段升级及后台审核流程自动化测试，连同阶段一用例共7项；后台完整回归51项全部通过，39个JavaScript文件语法检查全部通过。隔离环境真实浏览器完成未登录拦截、通过、拒绝、管理员回复、事后隐藏和二层深度限制操作；桌面无横向溢出/控制台错误，390px视窗内容宽375px、无横向溢出。阶段三再接入前台提交与已批准内容展示。

## 2026-08-08 反馈评论系统三阶段计划第一阶段：数据层与公开提交

- SQLite新增 `feedback_comments` 表，保存顶层留言/回复关系、称呼、可选邮箱、正文、审核状态、创建/批准时间和仅供限流及滥用追踪的IP地址；状态限定为 `pending/approved/rejected`，`parent_id` 使用自引用外键。
- 新增独立反馈服务与无需登录、无需CSRF的 `POST /api/feedback/comments`。称呼为1至80字符、可选邮箱不超过254字符并校验格式、正文为2至2000字符；回复只能指向已批准的顶层留言，拒绝不存在、待审核、已拒绝或本身为回复的目标。
- 所有真实提交一律写入 `pending` 审核队列，不触发静态发布，也没有公开读取接口。`website` 蜜罐字段有内容时返回与正常提交相同的 `202` 响应但不写库，避免向自动化爬虫暴露识别结果。
- 公开提交复用 `express-rate-limit` 的按IP模式，默认每15分钟5次，可通过独立环境变量调整；反馈API同时免于创建无意义的管理员session。`feedback.html`和后台管理页面本轮均未修改。
- 新增5项端到端自动化测试，覆盖顶层留言、已批准留言回复、蜜罐静默拒绝、IP限流、非法父级/多层回复，以及空值、超长正文和错误邮箱；后台完整回归49项全部通过，全仓39个JavaScript文件均通过语法检查。

## 2026-08-08 v1.5 编辑作品重构三阶段存档

- 将编辑作品重构的三个阶段作为单一 `v1.5` 提交推送到 [GitHub仓库](https://github.com/z987645344-arch/zhiliaohub) 的 `main` 分支，并创建同名带注释标签。
- `works` 表新增封面、下载开关/文件、体验链接、主媒体类型/路径、辅图JSON和版本日志8个字段；旧分类真实迁移为“程序/影视/生活”，新旧数据库结构保持兼容。
- 后台新增独立作品表单、原生Canvas 16:9封面裁剪、主图/主视频、多辅图和ZIP上传；发布服务把受引用媒体复制到前台受控目录，清理失效副本并在失败时恢复原状态。
- 前台作品列表与详情接入Steam风格展示、图片封面回退、主/辅媒体切换、Markdown版本日志和条件化下载/体验按钮，并移除作品详情“登录入口”。作品下载由每条作品独立开关控制，作为公开静态资源提供，不增加额外访问限制。
- 存档前44项后台自动化测试、37个JavaScript文件语法检查、15个前台HTML本地引用检查及桌面/390px真实浏览器回归均通过。当前仍未真实部署，没有域名、HTTPS或异地备份；8个正式作品仍为占位内容，真实素材由用户后续通过管理后台填写。

## 2026-08-08 编辑作品重构阶段三·完成：Steam风格前台展示

- 作品列表接入真实封面：有 `cover_image` 时渲染本地图片封面，无封面时继续使用原CSS几何封面；卡片标签直接显示“程序/影视/生活”分类，描述统一使用必填详情简介。
- 作品详情改为两段式Steam风格展示：上方为主图/主视频、横向辅图条和标题简介信息区，下方独立渲染Markdown版本日志；辅图缩略图可在原生JavaScript中切换主图区的图片或视频，768px以下自动单列堆叠。
- 移除作品详情“登录入口”。下载按钮仅在作品开启下载且存在ZIP文件时出现，并直接下载发布到 `assets/works/downloads/` 的真实文件；体验按钮仅在配置体验链接时出现并在新窗口打开。下载文件为公开静态资源，每个作品独立开关控制，本轮不增加登录或其他访问限制。
- 保留旧 `detail-hero`、`detail-actions`、`empty-log` 等样式以兼容其他潜在引用；8个无媒体旧作品均正确回退CSS封面和空版本日志，不显示空按钮或产生脚本错误。
- 后台44项自动化测试、37个JavaScript文件语法检查、16个生成/手写HTML本地引用检查全部通过。真实创建并发布含封面、主图、2张辅图、ZIP、体验链接和版本日志的临时作品，验证封面、缩略图切换、真实下载和新窗口体验均正常；随后删除临时作品并全量发布，恢复8个作品、3篇日记和0个前台媒体副本。
- 真实浏览器逐页检查8个正式作品与临时媒体作品：桌面端和同源390px视口均无横向溢出、showcase在窄屏正确单列堆叠、控制台无错误，所有详情页均已移除“登录入口”。三阶段“编辑作品重构”至此完成，改动仍未提交，等待用户确认。

## 2026-08-08 编辑作品重构阶段二：后台媒体表单与发布复制

- 将作品表单从作品/日记共用表单中拆出，日记继续沿用原表单；作品表单按基础信息、封面、展示媒体、下载设置和版本日志分区，分类改为“程序/影视/生活”下拉，管理面板作品列表改为显示分类。
- 新增不依赖第三方库的原生 Canvas 16:9 封面裁剪与 Fetch 上传流程；主图/主视频、多个辅图/辅视频和ZIP下载文件继续复用既有认证、CSRF、扩展名/MIME/签名白名单及大小限制，上传预览只对已登录管理员开放。
- `createWork` / `updateWork` 已接入封面、可下载开关、下载文件、体验链接、主媒体类型/路径、辅图JSON和版本日志全部字段；媒体路径只接受四个受控的 `assets/works/` 子目录及匹配类型扩展名。
- 全量发布会从后台 `uploads/` 收集数据库实际引用的媒体，复制到前台 `assets/works/covers`、`main`、`gallery`、`downloads`；不再被任何作品引用的前台媒体副本会自动删除，发布失败时恢复发布前的HTML和媒体状态。后台上传源文件仍保留，避免误删可能被其他记录复用的原始文件。
- 新增上传+发布媒体完整流程和独立作品表单测试：覆盖全部字段新增、媒体复制、换主图/减辅图后的失效副本清理、作品删除后的前台媒体清理，以及浏览器脚本语法、Canvas、Fetch、CSRF和最小CSP约束；后台44项自动化测试全部通过。
- 真实浏览器联调发现并修复三项不能由静态推断替代的问题：原CSP的 `script-src 'none'` 阻止整个表单脚本、`img-src` 缺少 `blob:` 阻止本地封面预览，以及初版裁剪框只能移动不能缩放。最终策略只允许同源脚本和图片 `blob:`；裁剪框可拖动内部移动、拖动四角按16:9缩放，用户实际操作确认正常。
- 在真实SQLite和真实前台目录完成测试作品新增→上传主图/2张辅图/ZIP→补充并重新裁剪封面→分类改为生活、移除主图和一张辅图→删除作品；每次保存均全量发布，旧主图、旧辅图、旧封面及删除后的全部前台媒体副本均被清理，最终恢复8个作品、3篇日记、前台媒体0个，SQLite `integrity_check=ok`。后台上传源文件按设计保留且不纳入前台清理。
- 真实浏览器在1280px与390px检查完整编辑表单和裁剪器，均无横向溢出、无控制台警告或错误。本阶段没有修改前台作品模板、`css/style.css` 或 `js/site.js`；上传媒体暂不在访客端展示，留待第三阶段接入。

## 2026-08-08 编辑作品重构阶段一：数据、分类与上传边界

- `works` 表新增 `cover_image`、`is_downloadable`、`download_file`、`experience_url`、`main_media_type`、`main_media_path`、`gallery`、`version_log` 8个可空字段；新库schema与旧库 `addMissingColumns` 保持一致，旧数据无需补默认值。
- 依据真实SQLite而非设计稿推测值完成一次性分类迁移：影像创作、AI音乐、AI影像、三维建模归入“影视”，网页设计、软件、AI系统归入“程序”；真实结果为影视4条、程序4条、生活0条，并通过 `content_migrations` 标记防止重复执行。
- 内容服务新增固定分类枚举、分类校验、辅图JSON校验与安全解析函数；作品新增和编辑开始要求 `detail_intro` 必填。本阶段尚未把8个媒体字段接入后台作品表单。
- 上传白名单新增ZIP及PK文件头校验；统一上传上限由25MiB调整为100MiB，兼顾视频/压缩包开发需求与默认磁盘滥用风险，暂不引入按文件类型拆分的独立上限。
- 真实迁移前创建本地备份 `backup-20260808T055205198Z.tar.gz`。全量发布后 `works.html` 不变，8个作品详情页仅出现已确认的分类文字变化；模板、页面结构、按钮、正文、CSS和JavaScript均未因本阶段改动。
- 后台隔离回归共40项通过，新增覆盖旧库8字段升级、迁移防重复、分类/辅图校验、简介必填及ZIP签名；全仓JavaScript语法检查通过。

## 2026-08-07 v1.4 静态内容发布与App局域网联调存档

- 将作品/日记保存即静态发布、11条既有内容迁移和 `admin-server` 局域网访问支持作为单一 `v1.4` 提交，推送至 [GitHub仓库](https://github.com/z987645344-arch/zhiliaohub) 的 `main` 分支并创建同名带注释标签。
- v1.4包含统一模板、全量发布、稳定slug、旧生成页清理、SQLite+Markdown内容源和13个生成HTML；访客仍只读取发布完成的静态文件，前台保持零运行时后端依赖。
- 后台支持通过明确配置 `HOST=0.0.0.0` 在可信任局域网监听，并自动列出非虚拟RFC1918 IPv4访问地址；配套App已完成无 `adb reverse` 的同一WiFi真机验证，但App代码和版本标签继续由独立仓库维护。
- 密码哈希工具、密码+TOTP、P-256设备认证、SQLite session及本地备份恢复等既有能力保持不变；本版本仍未真实部署，没有域名、HTTPS、异地备份或生产恢复演练。

## 2026-08-07 App 同一 WiFi 局域网直连完成

- `admin-server` 启动时新增网络接口探测：只列出非回环、非常见虚拟网卡的 RFC1918 IPv4；监听全部接口时打印每个真实局域网访问地址，仅监听回环时明确提示局域网尚未启用，避免把候选 IP 误报为可访问。
- `.env.example` 和后台 README 补充可信任家庭网络下的 `HOST=0.0.0.0` 配置、防火墙专用网络边界，以及不提供 mDNS、自动发现或 IP 自动更新的范围说明。
- 新增3项网络地址单元测试，后台完整回归34项全部通过，Node.js语法检查通过。
- 真实 `.env` 已经用户明确授权改为 `HOST=0.0.0.0`；本机回环和 WLAN 地址访问 `/health` 均返回 `ok`，启动地址探测正确排除 Cloudflare WARP 虚拟接口。
- Vivo V2405A 在无 `adb reverse` 情况下通过同一 WiFi 完成已有设备挑战登录，修改地址无需重新配对，健康状态在线；关闭 WiFi 显示超时，重新开启后恢复正常。
- 联调中首次以 Codex 沙箱权限启动后台时，现有 SQLite 主库写入受限导致 challenge 返回 HTTP 500；改用真实用户权限启动后接口恢复 `201`。该现象属于测试进程权限配置，不是服务端认证逻辑缺陷。

## 2026-08-06 作品/日记保存即静态发布完成

- 将原手工维护的8个作品与3篇占位日记迁入真实SQLite和Markdown，保留原有稳定URL、标题、分类、摘要、详情正文及“知天”官方地址待开放特殊状态；一次性迁移带只读预览、空表/目标文件/重复执行保护，并要求后台停止后显式确认。
- 新增统一作品/日记列表与详情模板，以及串行化全量发布服务；Markdown由锁定版本的 `marked` 渲染，元数据转义、原始HTML转义并拒绝危险协议链接/图片地址。
- 后台新增、编辑、删除现在保存即发布，管理面板显示已发布状态与最近发布时间，并支持手动全量重发；稳定slug处理非ASCII标题和重名冲突，标题编辑不改变现有URL。
- 每个生成HTML首行写入“请勿手动编辑”标记；发布只写 `works.html`、`notes.html` 和作品/日记详情页，只清理带标记的过期详情页，不修改首页、反馈页、CSS或JS，写入失败会恢复发布前文件。
- 隔离自动化测试增至31项并全部通过，覆盖新增→编辑→删除的SQLite/Markdown/静态HTML同步、旧页清理、迁移、防重复、后台端口占用拒绝、slug冲突、输出安全和非目标文件保护；Node.js静态语法检查通过。
- 真实数据库迁移后为8个作品/3篇日记、13个生成页面且标记完整；在真实数据和真实前台目录完成临时作品新增→编辑→删除并恢复8/3。浏览器逐页检查13个页面在1280px与390px下均无横向溢出、无控制台警告或错误；现有CI的HTML本地引用检查在生成结果上通过。
- 收尾发现首次端口检查因系统权限被静默忽略，导致真实迁移时旧admin-server进程实际上仍在监听；当时内容表为空且未观察到并发写入。随后已停止进程并确认SQLite `integrity_check=ok`、无外键异常、8/3记录及13/13页面语义一致；迁移脚本已新增主动TCP端口探测，检测到后台监听时直接拒绝执行。
- 前台仍保持零运行时后端依赖：访客只读取发布完成的静态HTML；下载、前台登录/编辑、反馈/评论和知天正式入口仍未开放。

## 2026-08-06 v1.3 密码哈希工具与App兼容性说明

- 新增 `admin-server/scripts/hash-password.js`，通过交互式终端输入并二次确认管理员密码，以 bcrypt cost 12 生成用于 `.env` 中 `ADMIN_PASSWORD_HASH` 的哈希。
- 密码输入不回显，不作为命令行参数进入历史，不写日志或文件；标准输出只包含最终 bcrypt 哈希。
- `admin-server/README.md` 的本地配置步骤改用该工具，并新增“配套App兼容性”章节：设备认证接口自 `v1.2` 提供，已验证兼容独立仓库 `zhiliaohub_app v0.1`。
- 主站与配套App继续各自独立打版本标签，不强行对齐版本号；后续破坏性接口变更通过兼容性说明维护最低版本配对关系。
- 新脚本及全仓共23个JavaScript文件通过 Node.js 语法检查；后台28项自动化测试全部通过。

## 2026-08-04 v1.2 部署前准备与设备认证登录存档

- 将部署前准备与设备认证能力作为一次完整版本提交：session改为SQLite持久化存储，服务重启后未过期登录态可继续使用，现有密码+TOTP保留为并行备用入口。
- 纳入带manifest与SHA-256校验、可选AES-256-GCM加密及保留策略的本地备份/恢复机制；已验证破坏数据后的完整恢复，但当前仍无定时调度和异地备份。
- 纳入非root多阶段Dockerfile、构建排除规则与需人工合并的Compose/nginx部署片段；本版本仍未连接真实服务器、域名、HTTPS证书或共享部署配置。
- 纳入单设备配对、P-256挑战应答登录、重放防护、吊销与自动替换能力；配套安卓App继续在独立仓库 `zhiliaohub_app` 开发，本仓库不包含App代码。
- 当前状态以 `v1.2` 提交并推送至 `https://github.com/z987645344-arch/zhiliaohub`，同时创建同名带注释标签；GitHub Actions仍只执行JavaScript语法与HTML本地引用检查，不扩展后台测试范围。

## 2026-08-04 设备配对与挑战应答登录完成

- 在保留密码+TOTP备用入口的前提下新增并行设备认证：已通过密码+TOTP的session可生成5分钟一次性配对码，公开配对接口只接受有效配对码与ECDSA P-256 SPKI公钥，数据库不保存设备私钥或配对码明文。
- 新增设备、配对码与挑战表以及后台“设备管理”页面；当前只允许一个有效设备，新配对自动吊销旧设备，主动吊销同时使旧公钥、未完成挑战和该设备建立的session失效。
- 新增短期随机挑战与 `SHA256withECDSA` 验签接口，签名载荷带固定协议上下文，成功后原子消费挑战并复用现有SQLite session；错误签名不消费挑战，成功挑战禁止重放。
- 新增安卓App对接接口文档与环境变量说明；本轮不实现二维码或App代码，配套App计划在独立仓库 `zhiliaohub_app` 开发。
- 完整测试由22项增至28项并全部通过，覆盖配对码权限/过期/重用、正确与错误签名、挑战重放/过期、吊销、重新配对、自动替换和现有密码+TOTP回归；前台15个页面未修改。

## 2026-08-04 管理后台部署前准备完成

- 将 `express-session` 默认MemoryStore替换为复用现有better-sqlite3数据库的持久化Store，新增session表、过期读取/周期清理和可配置有效期；实测未过期登录态可跨服务重启保留，过期管理员身份会失效。
- 新增备份与恢复命令，覆盖SQLite一致性快照、作品/日记Markdown和上传文件；归档内manifest记录字节数与SHA-256，支持scrypt + AES-256-GCM可选加密及默认最近7份保留策略。
- 自动化测试真实完成“加密备份→错误密码拒绝→破坏SQLite/Markdown/上传数据→恢复→一致性确认”和保留策略验证；完整后台测试由18项增至22项并全部通过。
- 新增非root多阶段 `admin-server/Dockerfile`、`.dockerignore` 和 `deploy/README.md`，提供需人工合并的Compose/nginx片段及全部真实环境占位项；本机没有Docker CLI，因此未执行镜像构建，也未修改任何服务器配置。
- 当前仍未实际部署；本地手动备份不能替代定时调度、异地副本、失败监控与服务器恢复演练，真实服务器/域名/证书就位后须由用户或运维人员人工合并共享配置。

## 2026-08-04 v1.1 管理后台存档与测试覆盖优化

- 将原先2个混合端到端用例拆成按风险命名的测试与子用例，`npm test` 共18项通过；失败时可直接定位未登录、密码、TOTP绑定/验证/重放、CSRF、session篡改、限流、内容、原子写入、加密或上传场景。
- 补齐TOTP密钥AES-256-GCM加解密及篡改拒绝、上传内容签名伪装、同一作品/日记并发更新一致性等缺口；内容服务按记录在单进程内串行更新，明确不把它描述为多实例并发能力。
- 在 `admin-server/README.md` 固化已覆盖与尚未覆盖的回归风险清单；现有GitHub Actions继续只执行JavaScript语法与HTML本地引用检查，本轮不擅自安装后台依赖或运行后台测试。
- `.gitignore` 新增本地概念图目录 `assets/concepts/`，并复核真实 `.env`、SQLite数据库、`node_modules` 与上传文件继续排除在版本控制之外。
- 当前后台与相关文档作为 `v1.1` 推送至 `https://github.com/z987645344-arch/zhiliaohub`；仍仅完成本地开发验证，没有真实部署、生产会话存储或备份机制。

## 2026-08-04 管理后台后端完成本地开发与验证

- 新增物理隔离的 `admin-server/` Node.js/Express 服务及独立 `package.json`，提供服务端登录、TOTP绑定/验证、作品与日记管理表单和受限文件上传入口；根目录15个前台页面未修改，也尚未接入后台数据。
- 使用 bcrypt 环境变量哈希、加密保存的TOTP密钥、httpOnly/SameSite会话Cookie、CSRF令牌和按请求IP统计的失败限流保护唯一管理员；明确不提供注册和“失败N次锁定账号”机制。
- 使用 better-sqlite3 保存作品/日记结构化元数据，Markdown文件保存正文并通过同目录临时文件与原子替换防止半截写入；数据库、真实 `.env` 和上传文件均由 `.gitignore` 排除。
- 上传接口只允许代码中列明的图片、PDF、Markdown、音频和视频格式，同时检查扩展名、MIME、文件签名和大小上限；管理写接口全部要求已认证会话与CSRF令牌。
- 自动化端到端验证覆盖错误密码、TOTP二维码绑定、验证码成功/失败、IP限流、未登录拦截、作品/日记新增与编辑、原子写入中断、合规/超限/非白名单上传，并额外实际启动服务通过健康检查；本轮仅本地验证，未配置域名、服务器、HTTPS、nginx或备份机制。

## 2026-08-03 v1.0 首次正式存档与轻量CI建立

- 将当前15个静态页面、共享样式/脚本、都市烟雨插画和项目文档整理为唯一的创世提交并标记 `v1.0`；不伪造开发阶段历史，版本仍明确不包含后端、登录、下载或持久化能力。
- 关联私有 GitHub 仓库 `https://github.com/z987645344-arch/zhiliaohub`，以 `main` 作为默认协作分支；仓库可见性继续由用户在 GitHub 设置中管理。
- 新增 `.github/workflows/ci.yml`，在面向 `main` 的 push 和 pull request 上检查全部 JavaScript 语法与 HTML 本地相对引用，不引入浏览器、视觉回归或性能测试。
- `.gitignore` 排除用户提供的 `assets/reference/oc-three-view.jpg`，角色参考原图只留在本地；站点实际使用的4张压缩 WebP 插画正常纳入版本控制。
- README、项目记忆与技术架构同步 v1.0、远程仓库和CI边界；本轮不创建许可证，也不写入本地绝对路径到 README 或提交信息。

## 2026-08-02 多作品、占位日记与反馈评论界面扩展

- `works.html` 建立创作混剪视频、AI音乐、AI视频、建模、网页设计、知历、知了、知天8类作品卡片，并新增8个规律命名的独立详情页；所有详情页共用工作日志占位、返回入口及未开放操作样式。
- `notes.html` 改为含日期、标题、摘要和明确占位标记的可扩展日记列表，新增3个统一详情模板；编辑入口只提示“登录后可编辑，该功能暂未开放”，未引入账号能力。
- `feedback.html` 在原静态表单后新增评论输入、占位评论与缩进跟评结构，常驻说明内容不会保存、发送或展示；提交、评论和回复均由 `js/site.js` 阻止并显示准确的未开放提示。
- 新增并压缩 `works-oc-creative-passage.webp`、`notes-oc-rain-writing.webp`、`feedback-oc-message-slot.webp` 三张低饱和都市烟雨插画；角色设定沿用 OC 三视图，运行时不依赖生图服务。
- 真实浏览器逐项点击8个作品入口、3个日记入口及反馈交互；全部15个页面在1280×720与390×844下导航一致、无横向溢出或控制台错误，相对路径检查与 JavaScript 语法检查通过，未加入网络请求、下载或持久化能力。

## 2026-08-02 都市烟雨首页风格试做完成

- 将用户提供的 OC 三视图保存为 `assets/reference/oc-three-view.jpg`，据此生成低饱和都市雨巷插画并压缩为 62,250 字节的 `assets/hero-oc-rain-alley.webp`；画面仅作静态资源使用，无文字、水印或运行时外部依赖。
- `css/style.css` 将全站森林绿、纸张色和高饱和黄绿色替换为水泥灰、雾蓝灰与低饱和橄榄灰；共享导航、页脚、作品卡片、心得面板和反馈表单均保持清晰可读，未改其余三页内容结构。
- `index.html` 在开屏加入带准确替代文本的主视觉图片，调整遮罩与文字层级并移除会遮挡角色的圆形状态装饰，使桌面和窄屏都能保留角色与雨巷主体。
- `js/particles.js` 将浮动粒子连线改为雨线与缓慢雾团，继续限制数量、DPR最高为2和约45 FPS；减少动态效果、页面隐藏或首屏离开视口时停止持续绘制并保留静态雨雾画面。
- 真实浏览器验证首页插画加载、雨雾帧更新、视口外暂停/恢复、减少动态效果和页面隐藏静态化；四页在桌面与390px窄屏均无横向溢出、无新增控制台错误，JavaScript语法检查通过。

## 2026-08-02 项目骨架与首版视觉首页落地

- 初始化 Git 仓库及 `css/`、`js/`、`assets/`、`docs/` 静态站点结构，新增 `.gitignore`、`README.md` 与项目状态文档；坚持原生 HTML/CSS/JS，避免为当前规模引入构建链。
- 新增 `index.html`、`works.html`、`notes.html`、`feedback.html` 四个独立页面，通过共享样式与脚本统一导航、响应式布局和交互，不采用单页应用架构。
- 首页以原生 Canvas 实现自适应粒子浮动与连线效果，通过粒子数量上限、设备像素比上限、视口外暂停和减少动态效果偏好控制性能消耗。
- 作品页预留可扩展卡片网格并以 `#` 放置“知天”展示入口；反馈页仅提供静态表单，提交时明确告知功能未开放且内容不会发送或保存。
- 使用真实浏览器验证四页导航、首页动画、反馈提示及桌面/移动端布局，页面无控制台错误。
