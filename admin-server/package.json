{
  "name": "zhiliaohub-admin-server",
  "version": "0.1.0",
  "private": true,
  "description": "知了hub 独立本地内容管理后台",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node scripts/init-db.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check tests/admin-server.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^13.0.2",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "express-session": "^1.19.0",
    "helmet": "^8.3.0",
    "multer": "^2.2.0",
    "qrcode": "^1.5.4",
    "speakeasy": "^2.0.0"
  }
}
